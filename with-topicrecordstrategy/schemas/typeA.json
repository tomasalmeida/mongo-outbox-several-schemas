{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "typeA",
  "description": "EXAMPLE payload schema for 'typeA' (the Mongo `payload` object). Registered under subject '<topic>-typeA' (TopicRecordNameStrategy); the `title` must match the schema name the SMT stamps. additionalProperties:false keeps validation strict.",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "createdAt": { "type": "string" },
    "amount": { "type": "number" }
  },
  "required": ["id", "amount"],
  "additionalProperties": false
}
